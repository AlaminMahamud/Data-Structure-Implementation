// top variable value 

// top = 0 means one value is already stacked

// top = -1 means neither one value nor anything is stacked other wise we can say stack is empty


if(s.top==-1)
{
	// empty
}
else
{
	// not empty
}
